# 로컬 개발 기본 설정
APP_ENV=local
APP_DEBUG=true

# DB 설정 (v0.1: sync SQLite 기본, 운영 시 PostgreSQL URL로 교체 가능)
DATABASE_URL=sqlite:///./ashd.db
SECRET_KEY=change-me-in-local-dev

# 이메일(SMTP) 설정 - 필요 시 값 입력
SMTP_HOST=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=

# 텔레그램 봇 토큰 - 사용 시 입력
TELEGRAM_BOT_TOKEN=
